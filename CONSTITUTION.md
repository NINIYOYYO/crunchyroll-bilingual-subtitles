# 项目宪法（通用版）—— 可迁移、防退化、企业级协作规范

> 本宪法是项目代码与 AI 协作的最高准则。  
> **目标**：无论代码可见度多少、无论开发环境如何变化，始终产出架构清晰、边界坚固、可长期维护的代码，杜绝"代码石山"。

---

## 一、架构与分层

### 1.1 分层原则
- 代码必须遵守 **请求处理层 → 业务逻辑层 → 数据访问层 → 数据模型** 的四层单向依赖。
- 具体命名可配置，但层次结构必须存在且不可跳过。常见模式如：
  - `handler / controller → service / usecase → repository / dao → entity / model`
- **禁止反向依赖**：下层不得引用上层（如 `repository` 不得调用 `service`）。
- **禁止循环引用**：任何两个模块/文件间不得形成循环 `import` 链。

### 1.2 模块边界
- 系统按业务领域拆分为独立模块（如 `user`, `order`）。
- 模块间只能通过**公开接口**（模块根目录的公共导出文件）通信，**禁止导入对方内部实现细节**。
- 每个模块拥有完整的内部层次，可独立替换或重构。

### 1.3 单一职责
- 每个文件只做一件事，文件长度应控制在一个合理的阈值内（建议 200-300 行，按语言习惯调整）。
- 函数/方法保持短小，参数不宜过多（不超过 4 个），必要时封装为参数对象。

---

## 二、契约与类型安全

### 2.1 显式契约
- 所有公共接口（函数、方法、API）必须有明确的类型定义（静态类型语言必须；动态语言必须在注释/文档中说明类型）。
- 禁止使用无结构的基础类型作为复杂结构传递（如 JSON 的 `any` / `map[string]interface{}`），必须定义为具名结构体/类/DTO。
- 对外暴露的 API 需要同时维护**接口契约文档**（OpenAPI / GraphQL schema / protobuf 等），与代码同步更新。

### 2.2 破坏性变更
- 修改契约（如增减字段、改返回值类型）视为破坏性变更，需同步更新所有消费端，并在提交信息中注明。

---

## 三、依赖与副作用管理

### 3.1 纯函数与副作用隔离
- 业务规则逻辑应尽可能写成纯函数（无副作用、不修改入参）。
- 有副作用的操作（数据库、网络、文件系统、时间等）必须在独立的适配器层（如 `repository` / `adapter`）中封装，方便测试和替换。

### 3.2 依赖注入
- 对象不应自行创建它所依赖的东西，依赖应通过**构造函数、函数参数或工厂**显式传入。
- 使用语言/框架标准的依赖注入机制管理对象生命周期，**禁止在业务逻辑中直接 `new` 具体实现或调用全局单例**。

### 3.3 配置分离
- 所有环境配置（端口、密钥、地址等）从代码中完全抽离，集中于单独的配置目录。
- 代码通过统一的配置管理器读取，不同环境（dev/staging/prod）有独立的配置文件。
- **严禁硬编码**任何可能变化的字面量。

---

## 四、错误处理与日志

### 4.1 统一错误模型
- 定义项目级基础错误类（如 `AppError`），并按领域扩展子类（如 `NotFoundError`, `ValidationError`）。
- 业务逻辑中**禁止返回 `null`/`-1`/特殊字符串表示错误**，必须抛出/返回明确的错误对象。
- 最外层统一捕获错误，并转换为标准输出结构（如 `{ code, message, data }`）。

### 4.2 日志规范
- 通过统一的日志接口记录（不直接使用语言原生的 `print`/`console`）。
- 日志至少包含：级别、模块/类名、描述、关键参数（脱敏后）。
- **禁止记录**密码、令牌、完整身份证号等敏感数据。

---

## 五、测试与可验证性

### 5.1 测试覆盖要求
- 业务逻辑层的每个公共方法**必须有单元测试**，覆盖正常路径与主要异常情况。
- 每个 API 端点至少有一个集成/端到端测试。
- 测试文件应与源文件保持可对应的命名和目录结构。

### 5.2 测试即文档
- 测试用例名称必须表达清晰意图（如 `should_reject_duplicate_username`）。
- 复杂场景的测试需包含 Given-When-Then 结构注释。

---

## 六、代码风格与命名

### 6.1 命名约定（可配置）
- 文件名：统一使用 `kebab-case` 或 `snake_case`（项目初期选定，全局一致）。
- 类/接口/类型：`PascalCase`。
- 函数/变量：`camelCase` 或 `snake_case`（按语言习惯）。
- 常量：`UPPER_SNAKE_CASE`。
- 数据库字段：`snake_case`，与代码层通过映射转换。

### 6.2 注释原则
- 公共 API、复杂算法、非显而易见的业务规则**必须注释**，说明"为什么这样做"。
- 临时调整/技术债务标记需包含 `TODO/FIXME` 和创建日期。

---

## 七、AI 协作特殊条款（半古法编程核心）

### 7.1 看不见代码时的行为
- AI 只能修改**用户提供的文件内容**，或根据项目已有的 ARCHITECTURE.md 创建符合架构的新文件。
- 当需要调用外部模块但看不到代码时，必须在注释中**声明依赖契约**（例如 `// depends on: UserService.register({ email, password }): User`），而不是凭空臆造。
- 如果缺乏必要信息无法做出可靠决策，**AI 必须提问**，不能猜。

### 7.2 输出格式
- 所有代码输出必须使用标准文件标记 `// file: <相对路径>`，严格按 ARCHITECTURE.md 定义的目录结构放置文件。
- **【格式安全条款】多文件联合输出协议**：
  - 当一次性输出多个文件时，**严禁对单个文件分别使用嵌套的代码块标记（如 ```python ... ``` 或 ```text ... ```）**。
  - AI 必须将所有文件合并在 **一个单一的、最外层的代码块中** 进行统一输出，文件之间仅使用 `// file: <相对路径>` 进行行级物理隔离。
  - 这样做的目的是确保用户的“项目反解/生成工具”能够无差错、无干扰地切分出纯净的代码，避免在文件中混入 Markdown 的反引号等语法噪音。
- 生成新模块时，必须同时输出该模块的 `_MODULE.md`。
- 修改可能影响契约时，必须提醒用户更新 ARCHITECTURE.md。

### 7.3 宪法更新
- 本宪法随项目迭代可修改，但必须在提交信息中注明变更，并且**所有后续 AI 会话需重新加载最新版**。
